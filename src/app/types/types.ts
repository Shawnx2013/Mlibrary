export interface Movie{
    title: string;
    publishDate: string;
    length: number;
    Description: string;
}

export interface series{
    title: string;
    publishDate: string;
    numberOfEpisodes: number;
    description: string;
}